import { SkillsTooltipPipe } from './skills-tooltip.pipe';

describe('SkillsTooltipPipe', () => {
  it('create an instance', () => {
    const pipe = new SkillsTooltipPipe();
    expect(pipe).toBeTruthy();
  });
});
