import { FloatDigitOnlyDirective } from './float-digit-only.directive';

describe('FloatDigitOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new FloatDigitOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
