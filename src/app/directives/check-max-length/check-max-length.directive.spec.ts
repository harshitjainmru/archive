import { CheckMaxLengthDirective } from './check-max-length.directive';

describe('CheckMaxLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckMaxLengthDirective();
    expect(directive).toBeTruthy();
  });
});
