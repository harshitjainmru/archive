import { NullifyZeroDirective } from './nullify-zero.directive';

describe('NullifyZeroDirective', () => {
  it('should create an instance', () => {
    const directive = new NullifyZeroDirective();
    expect(directive).toBeTruthy();
  });
});
