import { CurrencyImagePipe } from './currency-image.pipe';

describe('CurrencyImagePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyImagePipe();
    expect(pipe).toBeTruthy();
  });
});
