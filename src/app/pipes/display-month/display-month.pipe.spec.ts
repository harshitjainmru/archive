import { DisplayMonthPipe } from './display-month.pipe';

describe('DisplayMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
