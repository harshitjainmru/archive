import { CheckTimesheetEditPipe } from './check-timesheet-edit.pipe';

describe('CheckTimesheetEditPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckTimesheetEditPipe();
    expect(pipe).toBeTruthy();
  });
});
