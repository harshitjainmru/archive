import { EmptyStatePipePipe } from './empty-state-pipe.pipe';

describe('EmptyStatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new EmptyStatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
