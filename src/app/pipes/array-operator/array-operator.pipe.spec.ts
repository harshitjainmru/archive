import { ArrayOperatorPipe } from './array-operator.pipe';

describe('ArrayOperatorPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayOperatorPipe();
    expect(pipe).toBeTruthy();
  });
});
