import { RoleSalaryPipe } from './role-salary.pipe';

describe('RoleSalaryPipe', () => {
  it('create an instance', () => {
    const pipe = new RoleSalaryPipe();
    expect(pipe).toBeTruthy();
  });
});
